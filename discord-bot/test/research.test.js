import test from 'node:test';import assert from 'node:assert/strict';import {validateResearchUrl,ResearchWorker} from '../src/agent/research.js';
test('research URL policy denies insecure/private hosts',()=>{assert.throws(()=>validateResearchUrl('http://example.com',{}));assert.throws(()=>validateResearchUrl('https://127.0.0.1/x',{}));});
test('research enforces content type before writing',async()=>{const worker=new ResearchWorker({repositories:{evidence:{}},policy:{timeoutMs:1000,maxBytes:10,allowedTypes:['text/plain'],allowedHosts:['example.com']},fetch:async()=>({ok:true,headers:new Headers({'content-type':'application/zip'}),arrayBuffer:async()=>new ArrayBuffer(1)})});await assert.rejects(worker.gather({taskId:'t',stepId:'s',urls:['https://example.com/x']}),/content type denied/);});
