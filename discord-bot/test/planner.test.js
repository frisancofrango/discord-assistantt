import test from 'node:test';import assert from 'node:assert/strict';import { PlanSchema } from '../src/agent/planner.js';
const step=(id,deps=[])=>({id,kind:'research',title:id,domain:'research',risk:'read',dependsOn:deps,input:{},preconditions:[],postconditions:[{description:'evidence exists',check:'evidence.count > 0'}],verification:{method:'persisted evidence',evidenceRequired:true},compensation:null});
test('plan schema accepts a valid DAG',()=>assert.equal(PlanSchema.parse({goal:'g',contextObservedAt:new Date().toISOString(),domain:'research',risk:'read',steps:[step('a'),step('b',['a'])]}).steps.length,2));
test('plan schema rejects missing and cyclic dependencies',()=>{assert.throws(()=>PlanSchema.parse({goal:'g',contextObservedAt:new Date().toISOString(),domain:'research',risk:'read',steps:[step('a',['b'])]}));assert.throws(()=>PlanSchema.parse({goal:'g',contextObservedAt:new Date().toISOString(),domain:'research',risk:'read',steps:[step('a',['b']),step('b',['a'])]}));});
