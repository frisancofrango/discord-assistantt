import test from 'node:test';import assert from 'node:assert/strict';import {Orchestrator} from '../src/agent/orchestrator.js';
test('orchestrator fails completion without evidence',async()=>{const task={id:'t',status:'pending',metadata:{}};const step={id:'s',position:0,status:'pending',input:{id:'one',kind:'tool',dependsOn:[],verification:{evidenceRequired:true},input:{}}};const repos={tasks:{get:async()=>task,update:async(_id,p)=>Object.assign(task,p)},taskSteps:{find:async()=>[step],update:async(_id,p)=>Object.assign(step,p)},taskCheckpoints:{create:async()=>{}}};const o=new Orchestrator({queue:{},repositories:repos,workers:{tool:async()=>({verified:true})},logger:{}});await assert.rejects(o.execute('t',{isCancelled:async()=>false,progress:async()=>{}}),/no persisted evidence/);assert.equal(task.status,'failed');});
